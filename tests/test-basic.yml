name: Test basic release (auto-generated tag)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Install mock gh CLI
        run: |
          mkdir -p "$HOME/.local/bin"
          printf '#!/bin/sh\necho "gh $*" >> /tmp/gh-calls.log\necho "gh called with: $*"\nexit 0\n' > "$HOME/.local/bin/gh"
          chmod +x "$HOME/.local/bin/gh"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - id: action
        uses: .
        with:
          title: "Test Basic Release"
          tag: "test-basic-v0.0.1"

      - name: Assert gh release create was called with correct args
        run: |
          echo "=== gh calls log ==="
          cat /tmp/gh-calls.log || echo "(no log file)"
          echo "===================="
          if [ ! -f /tmp/gh-calls.log ]; then
            echo "FAIL: gh was never called"
            exit 1
          fi
          if grep -q "release create" /tmp/gh-calls.log; then
            echo "PASS: gh release create was called"
          else
            echo "FAIL: gh release create was not called"
            exit 1
          fi
          if grep -q "test-basic-v0.0.1" /tmp/gh-calls.log; then
            echo "PASS: correct tag was used"
          else
            echo "FAIL: expected tag 'test-basic-v0.0.1' not found in gh call"
            exit 1
          fi
          if grep -q "Test Basic Release" /tmp/gh-calls.log; then
            echo "PASS: correct title was used"
          else
            echo "FAIL: expected title 'Test Basic Release' not found in gh call"
            exit 1
          fi
